version: 2

models:
  - name: email_sign_ups
    description: Contains a record for each web event with 'email_sign_up' event_name

    meta:
        contains_pii: false
        owner: Data Analytics
        subject_matter_expert: Rob Hill

    columns:
      - name: email_sign_up_event_wid
        description: Primary Key generated by the data warehouse, 
        tests:
          - unique
          - not_null

      - name: email_sign_up_event_id
        description: The web event ID, unique identifier for the event
        tests:
          - unique
          - not_null

      - name: email_sign_up_event_cookie_id
        description: < 
          Device identifier used to indicate a website visitor. For a new visitor, this value is set in the user’s browser cookies
        tests:
          - not_null

      - name: email_sign_up_event_customer_id
        description: <
          Unique identifier to indicate a customer. This value is null if the user is an anonymous web visitor

      - name: email_sign_up_event_utm_campaign
        description: The campaign that referred the user

      - name: email_sign_up_event_utm_medium
        description: The medium that referred the user

      - name: email_sign_up_event_utm_source
        description: The source that referred the user